services:
  bridge:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - logs:/app/logs
      - ${MEDIA_HOST_PATH:-./media/torrents}:/app/media/torrents
      - data:/app/data

volumes:
  logs:
  data:
